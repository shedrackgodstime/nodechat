// Vertical-only scroll container used across the app to prevent horizontal drift on mobile.

export component VertScroll inherits Flickable {
    // Callers provide `viewport-height`; locking the width removes horizontal panning.
    viewport-width: self.width;
}
