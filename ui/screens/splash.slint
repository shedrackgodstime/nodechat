import { AppTheme } from "../theme.slint";

export component SplashScreen inherits Rectangle {
    background: AppTheme.surface-primary;
    
    callback finished();

    VerticalLayout {
        alignment: center;
        spacing: AppTheme.sp-xl;

        // --- Logo Glow Spot ---
        Rectangle {
            width: 200px; height: 200px;
            
            // Subtle Glow
            Rectangle {
                width: 140%; height: 140%;
                background: @radial-gradient(circle, rgba(0, 122, 255, 0.15) 0%, transparent 70%);
            }

            VerticalLayout {
                alignment: center;
                Text { text: "💬"; font-size: 80px; horizontal-alignment: center; }
            }
        }

        VerticalLayout {
            spacing: AppTheme.sp-md;
            Text { text: "NodeChat"; font-size: 32px; font-weight: 900; color: #FFFFFF; horizontal-alignment: center; letter-spacing: -1px; }
            Text { text: "Secure P2P Mesh Network"; font-size: 14px; font-weight: 700; color: AppTheme.text-tertiary; horizontal-alignment: center; letter-spacing: 1px; }
        }

        // --- Minimal Loading Bar ---
        Rectangle {
            width: 120px; height: 4px;
            background: AppTheme.surface-secondary;
            border-radius: 2px;
            clip: true;

            Rectangle {
                x: 0; width: 40px; height: 100%;
                background: AppTheme.accent;
                border-radius: 2px;
                
                animate x {
                    duration: 1000ms;
                    easing: ease-in-out;
                    iteration-count: -1;
                }
            }
        }
    }
}

