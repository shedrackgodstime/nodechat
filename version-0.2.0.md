one thing i'll like to add in this version is peer auto discovery
the feature will be disabled by default, and the user can enable it in the settings

and this feature allow user id to be discoverable by others on the network, using the discovery method used by /home/kristency/Projects/rush-over-iroh project or if they is a better way.....

peer discovery and chose to connect with that peer if discover....

to make it sharp, peer use gossip method to quickly share the info with other nodes, so even when the node is offline it id can still be share... 
